# 技能

技能是 Agora 保存在本地的可复用 Markdown 指令文件。

开启“允许访问技能”后，每次新的生成只会冻结由文件名和描述组成的精简目录。模型可以通过技能工具列出、读取、创建、编辑或删除文件。技能正文不会自动塞进每一次请求；模型会在需要时按需读取相关文件。

Agora 不提供“当前技能”文件或当前技能开关。修改技能只影响之后开始的生成；已经准入的生成继续使用其冻结目录和工具策略。

原生导出与自动备份会包含技能。导入时会按照所选模式合并技能，或替换本地技能库。
